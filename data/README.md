# Data

Put competition CSV files here if you do not keep them in the repository root:

```text
data/raw/train.csv
data/raw/test.csv
data/raw/sample_submission.csv
```

The training scripts first look in the repository root and then in `data/raw/`.

